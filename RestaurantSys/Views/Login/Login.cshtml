@model RestaurantSys.Models.Member

@{
    ViewData["Title"] = "登入系統";
}

<h2 class="fw-bold">@ViewData["Title"] </h2>
<hr />
<div class="row">
    <div class="col">
        <form asp-area="" asp-controller="Login" asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MemberTel" class="control-label"></label>
                <input asp-for="MemberTel" class="form-control" />
                <span asp-validation-for="MemberTel" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" class="form-control" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <hr />
            <div class="form-group">
                <input type="submit" value="登入" class="btn btn-outline-dark" />
            </div>
            <div class="text-danger">
                @ViewData["Error"]
            </div>
        </form>
    </div>
</div>

<div class="mt-2">
    <a class="btn btn-outline-dark fw-bold" asp-controller="Menu" asp-action="Index">返回首頁</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
